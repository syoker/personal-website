import type { IconLinkProps } from './types/IconLinkProps';

import StyledIconLink from './components/StyledIconLink';

const IconLink = (props: IconLinkProps) => {
	return <StyledIconLink {...props} />;
};

export default IconLink;
